

# Core dependencies (should already have)
torch>=2.0.0
torchvision>=0.15.0
transformers>=4.35.0
Pillow>=10.0.0

# For monitoring and evaluation (NEW)
scikit-learn>=1.3.0
matplotlib>=3.8.0
numpy>=1.24.0
psutil>=5.9.0

# Optional: For advanced features
tqdm>=4.66.0  # Progress bars
tensorboard>=2.14.0  # Logging
joblib>=1.3.0  # Parallel processing

# Optional: For model distillation (future)
# distillation-lib>=1.0.0

# Optional: For quantization (future)  
# torch-quantization>=1.0.0
